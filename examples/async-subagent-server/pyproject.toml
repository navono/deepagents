[project]
name = "async-subagent-server-example"
version = "0.1.0"
description = "Self-hosted async subagent server example using deepagents"
requires-python = ">=3.11"
dependencies = [
    "examples-utils",
    "deepagents>=0.2.6",
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "langchain-anthropic>=0.3.0",
    "langgraph>=0.2.0",
    "langgraph-sdk>=0.1.0",
    "httpx>=0.28.0",
    "python-dotenv>=1.2.2",
    "langchain-openai>=1.2.2",
    "curl-cffi>=0.15.0",
]

[tool.uv.sources]
deepagents = { path = "../../libs/deepagents", editable = true }
examples-utils = { path = "../utils", editable = true }

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.0.3",
]
