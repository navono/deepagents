[project]
name = "task-assistant-demo-evals"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
test = [
    "pytest>=8.4.2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
