# Custom Provider Profile — LLM API Configuration
#
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# Then set the model in your experiment.toml:
#   model = "my-provider:your-model-name"

MY_PROVIDER_BASE_URL=
MY_PROVIDER_API_KEY=
